Full functional stack with Elixir/Phoenix in the backend, and elm for the frontend.
Communication with backend data via GraphQL (data stored in postgres database).
Frontend is styled using Tailwindcss.
